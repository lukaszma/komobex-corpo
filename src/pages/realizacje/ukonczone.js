import React from "react"
import Layout from "@components/layout/layout"

const RealizationsDone = () => (
  <div id="realizations-done" className="realizations-done">
    <Layout>UKOŃCZONE</Layout>
  </div>
)

export default RealizationsDone
